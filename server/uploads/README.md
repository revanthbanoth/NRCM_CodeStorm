This folder is used to store user-uploaded files (like PPTs and PDFs).
It must be kept in the project structure for the upload functionality to work correctly.
The .gitkeep file ensures this folder is tracked by Git even when it has no uploads yet.
